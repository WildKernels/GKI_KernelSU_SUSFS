name: 'Build Kernel'
description: 'Builds kernel using legacy build.sh or Bazel (kleaf)'
inputs:
  version:
    description: 'Kernel config version (e.g., android14-6.1)'
    required: true

runs:
  using: composite
  steps:
  - name: Setup Kernel Configs
    if: false
    uses: ./.github/actions/set-kernel-config
  
  - name: 'Provide host ld from repo prebuilts'
    shell: bash
    run: |
      # build.sh REPLACES PATH with 4 repo dirs (build-tools/path,
      # prebuilts-master/clang/.../bin, prebuilts/gas, out/host_tools), so
      # /usr/bin and /usr/local/bin are invisible: apt binutils can NOT fix
      # this. Instead, symlink ld.lld as "ld" inside the prebuilt clang's
      # own bin dir -- the clang driver looks there first, no PATH needed.
      # ("|| true": runner shell is -e -o pipefail; a failing find or a
      # SIGPIPE from head must not kill this step.)
      LDLLD=$(find "${{ github.workspace }}/kernel" -name "ld.lld" -type f 2>/dev/null | sort | head -n 1 || true)
      if [ -n "$LDLLD" ]; then
        echo "Using repo ld.lld: $LDLLD"
        ln -sf "$(basename "$LDLLD")" "$(dirname "$LDLLD")/ld"
        ls -la "$(dirname "$LDLLD")/ld"
      else
        echo "ERROR: no ld.lld anywhere under workspace/kernel:" >&2
        ls "${{ github.workspace }}/kernel"
        echo "--- prebuilts-master:" >&2
        find "${{ github.workspace }}/kernel/prebuilts-master" -maxdepth 4 2>/dev/null | head -n 20
        exit 1
      fi

  - name: 'Build Kernel'
    shell: bash
    working-directory: ${{ github.workspace }}/kernel
    run: |
      sed -i 's/check_defconfig//' ./common/build.config.gki
      if [ -f "build/build.sh" ]; then
        BUILD_GKI_ARTIFACTS="" \
        BUILD_GKI_CERTIFICATION_TOOLS=0 \
        BUILD_SYSTEM_DLKM=0 \
        SKIP_VENDOR_BOOT=1 \
        SKIP_EXT_MODULES=1 \
        SKIP_CP_KERNEL_HDR=1 \
        OUT_DIR="/home/runner/out" \
        LTO=thin \
        BUILD_CONFIG=common/build.config.gki.aarch64 \
        CC="/usr/bin/ccache clang" \
        CXX="/usr/bin/ccache clang++" \
        HOSTCC="/usr/bin/ccache clang" \
        HOSTCXX="/usr/bin/ccache clang++" \
        HOSTLDFLAGS="-fuse-ld=lld" \
        build/build.sh -j"$(nproc)"
      else
        sed -i '/name = "kernel_aarch64",/a\    check_defconfig = "disabled",' common/BUILD.bazel
        tools/bazel build \
          --config=fast \
          --disk_cache=/home/runner/.cache/bazel \
          //common:kernel_aarch64/Image
      fi

  - name: 'Gather Build Artifacts'
    shell: bash
    working-directory: ${{ github.workspace }}/kernel
    run: |
      set -euo pipefail
      if [ -f "bazel-bin/common/kernel_aarch64/Image" ]; then
        echo "Found Image at bazel-bin/common/kernel_aarch64/Image"
        cp "bazel-bin/common/kernel_aarch64/Image" "${{ github.workspace }}/AnyKernel3/Image"
        exit 0
        fi
      if [ -f "/home/runner/out/dist/Image" ]; then
        echo "Found Image at /home/runner/out/dist/Image"
        cp "/home/runner/out/dist/Image" "${{ github.workspace }}/AnyKernel3/Image"
        exit 0
      fi

      exit 1

